# 电商低价监控系统 交接文档

**日期**: 2026-06-09  
**项目路径**: `/home/lab-admin/price-monitor`  
**今日提交数**: 40+ commits  
**目标分支**: `dev`

---

## 一、项目当前架构

### 1.1 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 后端 | Python FastAPI + uvicorn | `:3001`，src/main.py |
| 前端 | Next.js 14 (App Router) | `:3000`，纯 Tailwind CSS，无 Ant Design |
| 数据库 | PostgreSQL | schema=`price_monitor`，直连 psycopg2，**不用 Prisma Client** |
| 部署 | Supervisor | `/home/lab-admin/price-monitor/supervisor.conf` |
| 推送 | 飞书 Webhook + Server酱微信 | push_service.py |
| 环境 | venv: `./venv/bin/python3`，DISPLAY=:0 | XAUTHORITY 见 supervisor.conf |

### 1.2 导航结构（5 项）

```
商品监控 → 预警中心 → 数据分析 → 系统设置 → 故障排查
```

---

## 二、v3.0 核心升级（以监控商品为中心）

### 2.1 新建数据库表

**执行过的 SQL**（已生效，无需重复执行）:

```sql
-- 新建 3 张表
CREATE TABLE "MonitorProduct" (id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL, brand VARCHAR(255), description TEXT, price_threshold_bag FLOAT, price_threshold_can FLOAT, price_threshold_mix FLOAT, sales_threshold FLOAT, whitelist_sellers TEXT DEFAULT '', created_at/updated_at TIMESTAMPTZ);
CREATE TABLE "ImportBatch" (id SERIAL PRIMARY KEY, monitor_product_id INT FK, file_name VARCHAR, import_time, total_count, ad_count, valid_count, imported_by INT);
CREATE TABLE "SkuCategory" (id SERIAL PRIMARY KEY, monitor_product_id INT FK, name VARCHAR, unit VARCHAR, conversion_factor FLOAT);

-- 扩展已有表
ALTER TABLE "Product" ADD COLUMN monitor_product_id, import_batch_id, seller_name, daily_sales, product_url, platform, shop_type, location, image_url;
ALTER TABLE "ProductSku" ADD COLUMN sku_image_url, sku_category_id, is_verified, updated_at;
ALTER TABLE "Alert" ADD COLUMN monitor_product_id, alert_value, threshold, is_handled, handled_at;
ALTER TABLE "SystemConfig" ADD COLUMN wechat_push_enabled, wechat_sendkey;
```

### 2.2 新建文件

| 文件 | 用途 |
|------|------|
| `src/api/monitor_products.py` | 监控商品 CRUD + SKU分类 + 导入(两阶段) + 商品列表 + 预警 |
| `src/services/sku_normalizer.py` | SKU 规格标准化（袋装/罐装/混合装自动匹配） |
| `src/services/sales_analyzer.py` | 日均销量计算 |
| `src/services/wechat_push_service.py` | Server酱微信推送 |
| `src/api/wechat.py` | 微信推送配置 API（GET status / PUT config / POST test） |
| `src/app/admin/monitor-products/page.tsx` | 监控商品列表页 |
| `src/app/admin/monitor-products/[id]/page.tsx` | 监控商品详情页（5标签） |
| `src/app/admin/monitor-products/[id]/import/page.tsx` | 独立导入页 |
| `src/app/admin/products/[id]/skus/page.tsx` | SKU 审核页 |
| `src/app/admin/analysis/page.tsx` | 价格分析页 |

### 2.3 删除文件（v3.1 精简）

| 删除文件 | 原因 |
|----------|------|
| `src/api/keywords.py` | 无意义模块 |
| `src/api/product_keywords.py` | 无意义模块 |
| `src/api/cron.py` | 暂时用不上 |
| `src/app/admin/keywords/page.tsx` | 无意义模块 |
| `src/app/admin/product-keywords/page.tsx` | 无意义模块 |
| `src/generated/prisma/` (整个目录) | Prisma Client 导致浏览器端 `os` 模块报错 |

### 2.4 关键 API 路由

| 方法 | 路径 | 功能 |
|------|------|------|
| GET/POST | `/api/monitor-products/` | 列表 / 新增 |
| PUT/DELETE | `/api/monitor-products/{id}` | 编辑 / 删除 |
| GET/POST | `/api/monitor-products/{id}/sku-categories` | SKU分类管理 |
| DELETE | `/api/monitor-products/{id}/sku-categories/{cat_id}` | 删除分类 |
| GET | `/api/monitor-products/{id}/imports` | 导入历史 |
| GET | `/api/monitor-products/{id}/products` | 该商品下的淘宝商品 |
| GET | `/api/monitor-products/{id}/alerts` | 该商品的预警 |
| POST | `/api/monitor-products/{id}/import` | 上传 Excel → 预览 |
| POST | `/api/monitor-products/{id}/import/confirm` | 确认导入 |
| GET | `/api/products/{product_id}` | 商品详情+价格历史 |
| GET/PUT | `/api/wechat/status` `/api/wechat/config` | 微信推送配置 |
| POST | `/api/wechat/test` | 微信测试推送 |
| GET/POST | `/api/alerts/list` `/api/alerts/batch-handle` | 预警管理 |
| GET/PUT | `/api/settings` | 系统设置 |

---

## 三、DTS 解析器改动（dts_parser.py）

在 `COLUMN_PATTERNS` 新增字段：
- `placeholder_type`: `["占位类型", "广告位类型", "展位类型"]`
- `seller_name`: `["掌柜名", "掌柜", "卖家昵称"]`
- `daily_sales`: `["日均付款人数", "日均销量"]`
- `image_url`: `["图片", "主图", "商品图片"]`

新增方法：
- `filter_natural_products(raw_products)` — 只保留 "自然位"
- `clean_ad_data(raw_products)` — 完整清洗（过滤广告+无效ID+补全链接+过滤0价格）

列匹配逻辑改为 **pattern 优先**：现价列的 pattern 在价格列之前，保证优先匹配 "现价" 而非 "原价"。

---

## 四、已知 Bug 修复记录

| Bug | 原因 | 修复 |
|-----|------|------|
| 监控商品无法保存 | 表单空字段 `""` → Pydantic `float\|None` 拒绝 | save() 中空字符串转 null |
| 权限拦截 | `_check_write_permission(role)` 不能直接做 Depends | 新增 `require_write_permission` 函数包装 |
| 浏览器报 `Cannot read properties of undefined (reading 'os')` | `src/generated/prisma/` 引用 Node.js os 模块 | npm uninstall prisma client + 删除整个 generated 目录 |
| Next.js 308 重定向导致 POST 失败 | API 路径末尾 `/` 缺失 | 前端 POST 路径加上 `/` |
| 导入预览无图片 | DTS 解析器未提取 image_url 列 | 新增 COLUMN_PATTERNS 映射 |
| token key 不匹配 | 前端读 `auth_token` 但某些页面用 `token` | 统一为 `auth_token` |

---

## 五、运维操作

### 5.1 日常命令

```bash
# 构建前端
cd /home/lab-admin/price-monitor && npm run build

# 重启所有服务（需要 sudo 密码）
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart all

# 查看服务状态
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf status

# 查看前端日志
tail -f /home/lab-admin/price-monitor/logs/nextjs-frontend.log

# 查看后端日志
tail -f /home/lab-admin/price-monitor/logs/fastapi-backend.log

# 测试 API（生成临时 token）
TOKEN=$(./venv/bin/python3 -c "import jwt,datetime,os;from dotenv import load_dotenv;load_dotenv();print(jwt.encode({'user_id':1,'username':'admin','role':'admin','exp':datetime.datetime.now(datetime.timezone.utc)+datetime.timedelta(hours=24)},os.getenv('JWT_SECRET',''),algorithm='HS256'))" 2>/dev/null)
curl -s http://localhost:3001/api/monitor-products/ -H "Authorization: Bearer $TOKEN"
```

### 5.2 Supervisor 配置

位置: `/home/lab-admin/price-monitor/supervisor.conf`  
已移除 `chrome-display` 进程，现只有 `fastapi-backend` 和 `nextjs-frontend`。

### 5.3 Crontab

当前仅保留 3 个任务：
```
0 */3 * * * cd ... && ./venv/bin/python src/scripts/check_alerts.py  # 预警检测
0 1 * * *   cd ... && pg_dump ...  # 每日备份
0 2 * * *   cd ... && find logs -mtime +7 -delete  # 日志清理
```

---

## 六、微信推送配置（Server酱）

1. 打开 https://sct.ftqq.com 微信扫码 → 获取 SendKey
2. Settings → 微信推送标签页 → 填入 SendKey → 启用
3. 点击「发送测试消息」验证

推送时机：预警触发时，`push_service.py` 同时推送到飞书和微信。

---

## 七、核心业务流

```
创建监控商品（设置规格+阈值）
  → 点击「导入数据」
    → 上传 DTS Excel
      → 自动清洗广告（只保留 "自然位"）
        → 预览列表（图片/价格/店铺，可取消勾选）
          → 确认导入
            → 入库（Product + ImportBatch + ProductHistory）
              → 触发预警检测
                → 飞书 + 微信推送
```

每个监控商品支持：
- 5 个标签页：基本信息 / 导入历史 / 商品列表 / SKU管理 / 预警记录
- 商品列表：筛选（价格/销量/掌柜/店铺/地址）+ 排序 + 分页 + 历史趋势图弹窗
- 白名单：按掌柜名排除，不参与预警
- 白名单商品仍显示但标记为「已排除」

---

## 八、注意事项

1. **不要引入 Prisma Client**：后端用 psycopg2 直连 SQL，Prisma schema 仅作文档参考
2. **不要引入 Ant Design**：前端全部 Tailwind CSS inline style
3. **不要删除 `.next/` 目录**：这是 Next.js 构建缓存，删除后需要重新 `npm run build`
4. **前端修改后必须 `npm run build` + 重启**：Next.js 生产模式不热更新
5. **Python venv 路径**: `./venv/bin/python3`，系统 python3 也有 PIL
6. **所有 xdotool/Chrome 相关代码已标记废弃**，系统现在是纯手动 Excel 导入模式
7. **`.env` 文件**位于项目根目录，JWT_SECRET 长度 < 32 bytes（有 InsecureKeyLengthWarning，可忽略）
