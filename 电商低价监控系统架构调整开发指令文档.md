电商低价监控系统架构调整开发指令文档
版本：v2.0
更新日期：2026 年 6 月 8 日
项目路径：/home/lab-admin/price-monitor
核心目标：放弃高风险自动抓取，改为手动 Excel 导入模式，聚焦数据解析、入库、预警核心功能，彻底解决淘宝反爬导致的稳定性问题
给 Claude 的前置提示词（必须先看）
你好，这是电商低价监控系统的架构调整开发指令文档，我需要你按照以下要求完成开发：
先通读本文档的所有内容，理解项目背景、调整原则、开发任务和验收标准
严格遵守文档中的所有约束和要求，不要自行添加任何额外功能或修改核心逻辑
先在 Plan Mode 下生成详细的执行计划，包括需要修改的文件列表、每个文件的修改内容、测试步骤
我审核计划无误后，你再切换到 Auto Mode 执行代码修改
所有代码修改必须严格遵守项目的 15 条核心公理（Python 类型注解、日志规范、权限校验、飞书格式等）
所有代码提交必须遵守.github/COMMIT_CONVENTION.md中的提交规范
一、项目背景与调整原则
1.1 调整背景
原系统自动抓取环节（纯 xdotool 物理操作）频繁触发淘宝反爬验证，滑块拦截率高、稳定性差，无法满足 7×24 小时生产环境要求。
1.2 核心调整原则
✅ 保留核心价值：按商品 ID 建立全量数据台账、价格 / 销量趋势分析、预警提醒、飞书推送
✅ 最大程度复用：现有 DTS Excel 解析、数据入库、预警检测、推送服务代码 100% 复用
❌ 彻底移除高风险模块：所有自动抓取、Chrome 管理、滑块验证相关代码
✅ 新增轻量入口：前端 Excel 上传 + 后端解析入库接口
✅ 严格遵守原有约束：所有代码必须符合项目交接文档中的 15 条核心公理
1.3 数据流变更
原流程：关键词搜索 → Chrome 物理操作 → DTS 自动导出 → 解析入库 → 预警
新流程：用户手动下载 DTS Excel → 前端上传 → 解析入库 → 实时 / 定时预警
二、后端开发任务（优先级：高）
2.1 新增 Excel 手动导入接口
文件路径：src/api/products.py
要求：
复用现有权限校验函数_check_write_permission()，仅管理员 / 主管可操作
复用src/services/dts_parser.py的parse_dts_excel()函数解析 Excel
复用src/services/product_service.py的batch_import_products()函数批量入库
导入成功后立即触发一次全量预警检测（调用src/scripts/check_alerts.py）
所有日志使用logging模块，禁止print
函数必须添加完整类型注解
完整代码实现：
python
运行
from fastapi import APIRouter, UploadFile, File, Depends, HTTPException
from src.services.auth_service import _check_write_permission
from src.services.dts_parser import parse_dts_excel
from src.services.product_service import batch_import_products
import logging
from typing import Dict, Any

router = APIRouter(prefix="/api/products", tags=["商品管理"])
logger = logging.getLogger(__name__)

@router.post("/import-excel", dependencies=[Depends(_check_write_permission)])
async def import_product_excel(file: UploadFile = File(...)) -> Dict[str, Any]:
    """
    手动导入DTS店透视导出的Excel文件，解析后批量入库并触发预警检测
    权限：仅admin/manager角色可访问
    """
    try:
        # 校验文件类型
        if not file.filename.endswith((".xlsx", ".xls")):
            raise HTTPException(status_code=400, detail="仅支持Excel文件格式（.xlsx/.xls）")
        
        # 解析Excel文件（复用现有DTS解析逻辑）
        parsed_data = parse_dts_excel(file.file)
        if not parsed_data or len(parsed_data) == 0:
            raise HTTPException(status_code=400, detail="Excel文件解析失败，未提取到有效商品数据")
        
        # 批量入库（复用现有商品服务）
        import_result = batch_import_products(parsed_data)
        
        # 导入成功后立即触发预警检测
        from src.scripts.check_alerts import check_all_alerts
        alert_result = check_all_alerts(test_mode=False)
        
        logger.info(f"Excel导入完成：成功{import_result['success_count']}条，失败{import_result['fail_count']}条")
        logger.info(f"预警检测完成：触发{alert_result['triggered_count']}条预警")
        
        return {
            "code": 200,
            "msg": "导入成功",
            "data": {
                "import_result": import_result,
                "alert_result": alert_result
            }
        }
    
    except Exception as e:
        logger.error(f"Excel导入失败：{str(e)}", exc_info=True)
        raise HTTPException(status_code=500, detail=f"导入失败：{str(e)}")
2.2 移除自动抓取相关代码
文件列表：
src/services/xdotool_crawler.py：标记为废弃（保留文件但添加注释说明）
src/services/human_behavior.py：标记为废弃
src/services/captcha_solver.py：标记为废弃
src/services/page_verifier.py：标记为废弃
src/api/taobao.py：保留登录接口但标记为废弃，注释说明不再使用
src/api/keywords.py：保留关键词管理接口，移除搜索相关接口
修改要求：
在每个废弃文件顶部添加注释：# 已废弃：2026-06-08 系统改为手动Excel导入模式，不再使用自动抓取功能
注释掉所有对废弃文件的导入和调用
不要删除文件，保留历史代码用于参考
2.3 修改定时任务配置
文件路径：src/api/cron.py
要求：
移除 "每 2 小时全量商品 SKU / 价格采集" 任务
保留以下三个定时任务：
每 3 小时价格 + 销量预警检测 + 飞书推送
每日 01:00 PostgreSQL 数据库全量备份
每日 02:00 自动清理 7 天前日志文件
确保 Python 路径使用./venv/bin/python，符合 A8 公理
三、前端开发任务（优先级：中）
3.1 新增 Excel 上传功能
文件路径：src/app/admin/products/page.tsx
要求：
在商品列表页顶部添加 "导入 DTS Excel" 按钮
点击按钮弹出上传弹窗，支持拖拽上传和点击选择文件
显示上传进度和导入结果（成功 / 失败数量、失败原因）
权限控制：仅管理员 / 主管可见该按钮（员工角色隐藏）
导入成功后自动刷新商品列表
核心代码实现：
tsx
"use client";
import { useState } from "react";
import { Button, Upload, message, Modal, Progress } from "antd";
import { UploadOutlined, ReloadOutlined } from "@ant-design/icons";
import axios from "axios";

export default function ProductsPage() {
  const [uploadVisible, setUploadVisible] = useState(false);
  const [uploading, setUploading] = useState(false);
  const [progress, setProgress] = useState(0);
  const userRole = localStorage.getItem("userRole");

  const handleUpload = async (file: File) => {
    setUploading(true);
    setProgress(0);
    
    const formData = new FormData();
    formData.append("file", file);
    
    try {
      const res = await axios.post("/api/products/import-excel", formData, {
        headers: {
          "Content-Type": "multipart/form-data",
          Authorization: `Bearer ${localStorage.getItem("token")}`,
        },
        onUploadProgress: (progressEvent) => {
          const percent = Math.round(
            (progressEvent.loaded * 100) / (progressEvent.total || 0)
          );
          setProgress(percent);
        },
      });
      
      message.success(
        `导入成功：${res.data.data.import_result.success_count}条商品，` +
        `触发${res.data.data.alert_result.triggered_count}条预警`
      );
      setUploadVisible(false);
      // 刷新商品列表
      window.location.reload();
    } catch (err: any) {
      message.error(err.response?.data?.detail || "导入失败，请检查文件格式");
    } finally {
      setUploading(false);
      setProgress(0);
    }
  };

  return (
    <div className="p-6">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-2xl font-bold">商品管理</h1>
        <div className="space-x-2">
          {userRole !== "staff" && (
            <Button
              type="primary"
              icon={<UploadOutlined />}
              onClick={() => setUploadVisible(true)}
            >
              导入DTS Excel
            </Button>
          )}
          <Button icon={<ReloadOutlined />} onClick={() => window.location.reload()}>
            刷新
          </Button>
        </div>
      </div>

      {/* 商品列表原有代码保持不变 */}

      <Modal
        title="导入商品数据"
        open={uploadVisible}
        onCancel={() => setUploadVisible(false)}
        footer={null}
        destroyOnClose
      >
        <div className="mb-4">
          <p className="text-gray-600 mb-2">
            请上传店透视（DTS）导出的Excel文件，系统将自动解析商品、价格和销量数据
          </p>
          <p className="text-orange-500 text-sm">
            注意：仅支持.xlsx和.xls格式，文件大小不超过10MB
          </p>
        </div>

        <Upload
          name="file"
          accept=".xlsx,.xls"
          beforeUpload={(file) => {
            const isValidType = 
              file.type === "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" ||
              file.type === "application/vnd.ms-excel";
            if (!isValidType) {
              message.error("仅支持Excel文件格式");
              return false;
            }
            if (file.size > 10 * 1024 * 1024) {
              message.error("文件大小不能超过10MB");
              return false;
            }
            return true;
          }}
          customRequest={({ file }) => handleUpload(file as File)}
          showUploadList={false}
          disabled={uploading}
        >
          <Button type="default" loading={uploading} disabled={uploading}>
            选择文件
          </Button>
        </Upload>

        {uploading && (
          <div className="mt-4">
            <Progress percent={progress} status="active" />
            <p className="text-gray-500 text-sm mt-2">正在上传并解析文件...</p>
          </div>
        )}
      </Modal>
    </div>
  );
}
3.2 简化前端无关功能
文件路径：src/app/admin/keywords/page.tsx
要求：
移除 "立即搜索" 和 "全部立即搜索" 按钮
保留关键词的添加、编辑、删除、启用 / 禁用功能
更新页面说明，说明关键词用于数据筛选而非自动搜索
文件路径：src/app/admin/diagnostics/page.tsx
要求：
移除 Chrome 状态监控、抓取日志查看相关模块
保留数据库备份、日志清理、服务重启（仅 FastAPI 和 Next.js）功能
文件路径：src/app/admin/settings/page.tsx
要求：
移除 "淘宝登录" 标签页
移除与自动抓取相关的配置项
四、运维配置修改（优先级：高）
4.1 Supervisor 配置修改
文件路径：/home/lab-admin/price-monitor/supervisor.conf
要求：
移除chrome-display程序配置块
保留fastapi-backend和nextjs-frontend配置
修改后执行以下命令生效：
bash
运行
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf update
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart all
4.2 Crontab 定时任务调整
执行命令：crontab -e
要求：
删除所有与自动抓取相关的任务
保留以下三个任务：
bash
运行
# 每3小时执行预警检测并推送飞书
0 */3 * * * cd /home/lab-admin/price-monitor && ./venv/bin/python src/scripts/check_alerts.py >> logs/alert.log 2>&1

# 每日凌晨1点数据库全量备份
0 1 * * * cd /home/lab-admin/price-monitor && ./venv/bin/python src/scripts/backup_db.py >> logs/backup.log 2>&1

# 每日凌晨2点清理7天前的日志文件
0 2 * * * find /home/lab-admin/price-monitor/logs -name "*.log" -mtime +7 -delete
4.3 清理临时文件
执行命令：
bash
运行
# 删除Chrome相关临时文件
rm -rf /home/lab-admin/.config/chrome-run-*
rm -rf /home/lab-admin/.config/chrome_base_template

# 清理旧的抓取日志
rm -rf /home/lab-admin/price-monitor/logs/crawl.log*
rm -rf /home/lab-admin/price-monitor/logs/chrome.log*
五、测试验收标准
5.1 后端接口测试
✅ 使用 Postman 调用/api/products/import-excel接口，上传 DTS 导出的 Excel 文件
✅ 验证返回结果包含成功 / 失败数量和预警触发数量
✅ 检查数据库Product和ProductHistory表，确认数据已正确入库
✅ 验证员工账号调用接口返回 403 权限错误
✅ 上传非 Excel 文件返回 400 错误
5.2 前端功能测试
✅ 管理员 / 主管账号可见 "导入 DTS Excel" 按钮
✅ 员工账号不可见导入按钮
✅ 点击上传按钮弹出弹窗，可选择文件并上传
✅ 上传过程中显示进度条
✅ 导入成功后显示成功消息并自动刷新商品列表
✅ 导入失败时显示错误消息
5.3 预警功能测试
✅ 导入包含低于预警价格商品的 Excel 文件
✅ 验证飞书收到预警推送（工作时段 9:00-18:00）
✅ 验证Alert表中已生成预警记录
✅ 验证同一商品 24 小时内不会重复推送预警
5.4 运维配置测试
✅ 执行sudo supervisorctl status，确认只有fastapi-backend和nextjs-frontend在运行
✅ 执行crontab -l，确认只有三个保留的定时任务
✅ 手动执行预警脚本，验证功能正常
六、注意事项
Excel 格式兼容：确保用户下载的 DTS Excel 格式与dts_parser.py中的列映射一致，如格式变更需同步更新解析逻辑
数据重复处理：batch_import_products函数已实现按商品 ID 去重，仅更新最新价格和销量，保留历史记录
权限控制：严格复用_check_write_permission()函数，确保员工只能查看数据，不能导入或修改
日志记录：所有导入和预警操作都已记录日志，便于问题排查
代码提交：所有修改必须按照.github/COMMIT_CONVENTION.md规范提交到 dev 分支

