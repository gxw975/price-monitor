
## 部署方式

```bash
# Supervisor 控制
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart fastapi-backend nextjs-frontend chrome-display
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf status

# 两个虚拟环境
.venv/bin/python   → Python 3.11.15（Supervisor 运行时）
venv/bin/python    → Python 3.12.3（crontab 定时任务）
```

---

## 2. 核心公理（不可更改的约束）

| # | 公理 | 说明 |
|----|------|------|
| A1 | **主工作区是服务器本地** | 所有开发编辑在 `/home/lab-admin/price-monitor`，GitHub 仅用于版本备份 |
| A2 | **仅已审核 + 非白名单商品** | 参与预警和 SKU 抓取 |
| A3 | **飞书推送时间限制** | 工作日 9:00-18:00，周末跳过 |
| A4 | **预警 24 小时去重** | 同一商品同一类型 24 小时内不重复推送 |
| A5 | **管理员/主管可写，员工只读** | 权限模型 via `_check_write_permission()` |
| A6 | **opencli 绝对路径** | `/home/lab-admin/.nvm/versions/node/v22.22.0/bin/opencli` |
| A7 | **飞书 API v2 格式** | 字段名 `msg_type`（带下划线），非 `msgtype` |
| A8 | **crontab 使用相对 venv** | `cd /path && ./venv/bin/python script.py` |
| A9 | **Python 3.10+ 语法** | 所有函数必须有类型注解 |
| A10 | **日志使用 logging 模块** | 禁止 print |
| A11 | **配置从 .env 读取** | 禁止硬编码 |
| A12 | **永久禁用 CDP 远程调试** | Chrome 不得使用 `--remote-debugging-port`，淘宝100%识别封锁 |
| A13 | **永久禁用自动化登录** | 不得在代码中硬编码账号密码或自动提交登录表单 |
| A14 | **永久禁用暴力杀进程** | 不得使用 `kill -9`/`pkill -KILL` 直接强杀 Chrome，必须优雅关闭 |
| A15 | **禁止绕过滑块验证** | 任何节点检测到滑块验证必须当场处理完毕再继续 |

---

## 3. 已完成的所有功能和修复

### 3.1 Chrome 浏览器管理（2026-06-06 重写）

**启动参数（永久固化，只保留6个）：**
```
google-chrome --start-maximized --no-first-run \
  --restore-last-session=false --disable-session-crashed-bubble \
  --disable-crash-reporter --ozone-platform=x11
```

**绝对禁止的参数：** `--remote-debugging-port`, `--remote-allow-origins`, `--disable-gpu`, `--disable-blink-features=AutomationControlled`, `--no-sandbox`

**守护进程：** `scripts/ensure-chrome.sh`（Supervisor `chrome-display` 程序托管）
- 每15秒检查 Chrome 进程存活
- 崩溃自动重启
- 启动前自动清理 SingletonLock 和 Preferences 崩溃标记

**优雅关闭流程：**
```
CDP 批量关闭全部标签页 → sleep 2s
pkill -TERM google-chrome → sleep 3s
仅残留进程才兜底 pkill -KILL
每次启动前修正配置：sed -i 's/"exit_type":"crashed"/"exit_type":"Normal"/' Preferences
```

### 3.2 抓取引擎（2026-06-06 重写为纯xdotool）

**核心技术栈：xdotool + python-xlib + xclip + openpyxl（零CDP）**

| 模块 | 文件 | 说明 |
|------|------|------|
| 核心爬虫 | `src/services/xdotool_crawler.py` | 全流程物理操作：Chrome管理、搜索、DTS导出、验证码求解 |
| 真人行为 | `src/services/human_behavior.py` | 自然鼠标轨迹、随机延迟、滚动模拟 |
| DTS解析 | `src/services/dts_parser.py` | 店透视导出Excel自动解析（15列自动映射） |
| 页面验证 | `src/services/page_verifier.py` | 每步操作前验证页面状态（窗口标题+截图像素分析） |
| 验证码求解 | `src/services/captcha_solver.py` | python-xlib XTest滑块拖拽（280+点正弦波轨迹） |

**DTS 自动加载精确参数（经验验证）：**
- 每次点击"自动加载" → 等待 **25秒**
- 每 **8次** 点击 → 暂停 **325秒**（防淘宝反爬风控）
- 最多 12 批循环，直到按钮消失
- 全程每5秒检查验证码

**数据流：** 搜索关键词 → DTS市场分析 → 分批自动加载 → 全选导出Excel → 解析入库

### 3.3 淘宝登录模块（2026-06-06 重写为OpenCLI方案）

| 端点 | 方法 | 文件 |
|------|------|------|
| `POST /api/taobao/start-login` | 打开登录页 + 截图二维码 | [taobao.py](src/api/taobao.py) |
| `GET /api/taobao/qrcode` | 获取最新二维码截图 | [taobao.py](src/api/taobao.py) |
| `GET /api/taobao/check-login` | 检测登录状态+封控 | [taobao.py](src/api/taobao.py) |

**登录方式：** OpenCLI 控制 Chrome 打开 `login.taobao.com`，截图返回二维码 base64，用户手机淘宝扫码。登录后 Cookie 由 Chrome 自然持久化。

### 3.4 关键词搜索

| 端点 | 方法 | 文件 |
|------|------|------|
| `POST /api/keywords/search` | 后台异步搜索（`keyword_id` 单个/null=全部） | [keywords.py](src/api/keywords.py) |
| `GET /api/keywords/search/status` | 轮询搜索状态 | [keywords.py](src/api/keywords.py) |

**前端**（keywords/page.tsx）：
- 右上角 "全部立即搜索" 按钮
- 每行 "🔍 立即搜索" 按钮
- 添加关键词后**自动触发搜索**
- 轮询状态：搜索中（蓝色）、完成（绿色）、失败（红色）

### 3.5 飞书推送

| 修复项 | 文件 |
|--------|------|
| `msgtype` → `msg_type`（4 处） | [push_service.py](src/services/push_service.py) |
| `urllib` → `requests` 库 | [push_service.py](src/services/push_service.py) |
| 返回 `tuple[bool, str]` 包含错误详情 | [push_service.py](src/services/push_service.py) |
| 测试推送返回 `dict` 结构 | [push.py](src/api/push.py) |

### 3.6 设置页面

| 修复项 | 文件 |
|--------|------|
| "个人微信配置"从预警 tab 移到推送 tab | [settings/page.tsx](src/app/admin/settings/page.tsx) |
| 预警阈值 tab 添加 "保存设置" 按钮 | [settings/page.tsx](src/app/admin/settings/page.tsx) |
| 推送 tab 添加 "保存推送设置" 按钮 | [settings/page.tsx](src/app/admin/settings/page.tsx) |
| 所有通道切换改为延迟保存（draftAlert 模式） | [settings/page.tsx](src/app/admin/settings/page.tsx) |

### 3.7 定时任务

| 修复项 | 文件 |
|--------|------|
| Python 路径统一为 `./venv/bin/python` | [cron.py](src/api/cron.py) |
| crontab 加 `cd /home/lab-admin/price-monitor && ` 前缀 | [cron.py](src/api/cron.py) |
| 日志/备份路径改为相对路径 | [cron.py](src/api/cron.py) |
| deploy.sh 去除 `source .venv/bin/activate`，改用 `./venv/bin/pip` | [deploy.sh](scripts/deploy.sh) |

### 3.8 认证

| 功能 | 文件 |
|------|------|
| JWT payload 新增 `openclaw_agent_id` 字段 | [auth_service.py](src/services/auth_service.py) |
| GET `/api/messages/command` 加入免鉴权路径 | [main.py](src/main.py) |

---

## 4. 当前系统状态

| 组件 | 状态 | 说明 |
|------|------|------|
| FastAPI 后端 | ✅ RUNNING | 端口 3001，Supervisor 托管 (`fastapi-backend`) |
| Next.js 前端 | ✅ RUNNING | 端口 3000，Supervisor 托管 (`nextjs-frontend`) |
| Chrome 守护 | ✅ RUNNING | Supervisor 托管 (`chrome-display`)，纯物理模式 |
| 淘宝登录 | ✅ 已登录 | 账号 `giftboy`，无封控 |
| 飞书推送 | ✅ 正常 | API v2 格式正确 |
| 关键词搜索 | ✅ 可用 | POST /api/keywords/search 异步执行 |
| DTS Excel解析入库 | ✅ 已实现 | dts_parser.py 自动解析15列 |
| 定时任务 | ✅ 配置完成 | crontab via API |
| 数据库 | ✅ 运行中 | PostgreSQL `price_monitor` schema |
| 前端构建 | ✅ 0 error | Next.js build 通过 |
| CDP 依赖 | ❌ 已彻底移除 | 全部改用 xdotool 物理操作 |
| SKU 抓取 | ⚠️ 待重设计 | 原CDP方案已废弃，纯xdotool方案待实现 |

---

## 5. 关键文件路径

### 后端 API

| 模块 | 路径 |
|------|------|
| 主入口 | `/home/lab-admin/price-monitor/src/main.py` |
| 认证 | `/home/lab-admin/price-monitor/src/api/auth.py` |
| 淘宝登录 ★ | `/home/lab-admin/price-monitor/src/api/taobao.py` |
| 关键词 ★ | `/home/lab-admin/price-monitor/src/api/keywords.py` |
| 推送 | `/home/lab-admin/price-monitor/src/api/push.py` |
| 设置 | `/home/lab-admin/price-monitor/src/api/settings.py` |
| 定时任务 | `/home/lab-admin/price-monitor/src/api/cron.py` |
| 预警 | `/home/lab-admin/price-monitor/src/api/alerts.py` |
| 商品 | `/home/lab-admin/price-monitor/src/api/products.py` |
| 诊断 | `/home/lab-admin/price-monitor/src/api/diagnostics.py` |

### 服务层

| 模块 | 路径 | 状态 |
|------|------|------|
| 核心爬虫 ★ | `src/services/xdotool_crawler.py` | 🟢 主力（纯xdotool） |
| DTS解析 ★ | `src/services/dts_parser.py` | 🟢 主力 |
| 页面验证 ★ | `src/services/page_verifier.py` | 🟢 主力 |
| 真人行为 | `src/services/human_behavior.py` | 🟢 主力 |
| 编排服务 | `src/services/ga_service.py` | 🟢 主力 |
| 验证码求解 | `src/services/captcha_solver.py` | 🟢 主力 |
| 商品入库 | `src/services/product_service.py` | 🟢 主力 |
| 认证服务 | `src/services/auth_service.py` | 🟢 主力 |
| 推送服务 | `src/services/push_service.py` | 🟢 主力 |
| CDP爬虫 | `src/services/cdp_crawler.py` | 🔴 废弃 |
| TagUI爬虫 | `src/services/tagui_crawler.py` | 🔴 废弃 |

### 自动化技能

| 模块 | 路径 | 状态 |
|------|------|------|
| 店透视导出 | `src/ga_skills/diantoushi_export.py` | 🔴 废弃（替代: xdotool_crawler.py） |
| SKU 抓取 | `src/ga_skills/sku_crawler.py` | 🔴 废弃（待重新设计） |

### 前端页面

| 页面 | 路径 |
|------|------|
| 仪表盘 | `/home/lab-admin/price-monitor/src/app/admin/dashboard/page.tsx` |
| 商品列表 | `/home/lab-admin/price-monitor/src/app/admin/products/page.tsx` |
| 商品详情 | `/home/lab-admin/price-monitor/src/app/admin/products/[id]/page.tsx` |
| 关键词监控 ★ | `/home/lab-admin/price-monitor/src/app/admin/keywords/page.tsx` |
| 预警中心 | `/home/lab-admin/price-monitor/src/app/admin/alerts/page.tsx` |
| 系统设置 | `/home/lab-admin/price-monitor/src/app/admin/settings/page.tsx` |
| 故障排查 ★ | `/home/lab-admin/price-monitor/src/app/admin/diagnostics/page.tsx` |
| 用户管理 | `/home/lab-admin/price-monitor/src/app/admin/users/page.tsx` |
| 日志查看 | `/home/lab-admin/price-monitor/src/app/admin/logs/page.tsx` |

### 配置与脚本

| 文件 | 路径 |
|------|------|
| Supervisor 配置 | `/home/lab-admin/price-monitor/supervisor.conf` |
| Chrome 守护 | `/home/lab-admin/price-monitor/scripts/ensure-chrome.sh` |
| 部署脚本 | `/home/lab-admin/price-monitor/scripts/deploy.sh` |
| 人机爬虫 | `/home/lab-admin/price-monitor/scripts/human_crawl.py` |
| 环境变量 | `/home/lab-admin/price-monitor/.env` |
| 数据库 Schema | `/home/lab-admin/price-monitor/src/prisma/schema.prisma` |
| 提交规范 | `/home/lab-admin/price-monitor/.github/COMMIT_CONVENTION.md` |

### 工具路径

| 路径 | 说明 |
|------|------|
| `/usr/bin/google-chrome-stable` | Chrome 浏览器 |
| `/home/lab-admin/.nvm/versions/node/v22.22.0/bin/opencli` | OpenCLI 浏览器控制 |
| `/home/lab-admin/.nvm/versions/node/v22.22.0/bin/openclaw` | OpenClaw Agent |
| `/usr/bin/xdotool` | X11 键盘鼠标模拟 |
| `/home/lab-admin/GenericAgent/` | GA 框架安装目录 |

---

## 6. 数据库结构

| 表 | 核心字段 |
|----|---------|
| **User** | id, username, password, role(admin\|manager\|staff), `openclaw_agent_id` |
| **Product** | `product_id`(PK), title, shop_name, `is_approved`, `is_whitelist`, `last_sku_crawled_at` |
| **ProductHistory** | product_id(FK), price, `sales_volume`, recorded_at |
| **ProductSku** | product_id(FK), `sku_name`, `sku_price`, `unit_price`, recorded_at |
| **Keyword** | id, name, platform(taobao\|tmall\|jd), `is_active`, created_by(FK→User) |
| **ProductKeyword** | keyword_id + product_id（联合主键） |
| **Alert** | product_id, `alert_type`, message, `is_sent`, `is_read` |
| **SystemConfig** | alert_price, work_start/end_hour, `feishu_webhook`, `wechat_webhook`, `push_enabled_channels`, crawl 相关 |
| **OperationLog** | user_id, username, action, target, path, ip |

---

## 7. 废弃路径（已否决方案，禁止重提）

| # | 方案 | 原因 |
|----|------|------|
| D1 | 用 `"msgtype"` 作为飞书字段名 | API v2 要求 `"msg_type"`（带下划线） |
| D2 | 用 `urllib.request` 发 HTTP 请求 | 改用 `requests` 库，错误处理更好 |
| D3 | 直接 `"opencli"` 字符串调用 | Supervisor 环境无 nvm PATH，必须用绝对路径 |
| D4 | 绝对路径写死 Python 解释器 | 改用 `cd + ./venv/bin/python` 相对路径 |
| D5 | crontab 中用绝对路径 | 改用 `cd /path && ./venv/bin/python` |
| D6 | Chrome `--remote-debugging-port` 参数 | **淘宝100%识别并封锁搜索结果渲染** |
| D7 | CDP / Selenium / Playwright | 淘宝检测自动化框架，搜索结果不显示 |
| D8 | 自动化填表登录 | 触发淘宝盗号风控，直接冻结账号 |
| D9 | 绕过滑块验证抓取 | 残缺数据 + 触发更严重风控 |
| D10 | 暴力 `kill -9` Chrome | 下次启动弹"要恢复页面吗"，插件异常 |
| D11 | 无头模式 / Xvfb 虚拟显示 | 淘宝检测无真实渲染环境 |
| D12 | 两个 venv 混用 | `.venv`（3.11.15）用于 Supervisor，`venv`（3.12.3）用于 crontab，不能混用 |
| D13 | 设置标签页的通道切换立即保存 | 已改为按钮保存（draftAlert 模式） |

---

## 8. 下一步待办

| # | 事项 | 优先级 | 状态 |
|----|------|--------|------|
| T1 | 关键词搜索完成后自动解析导出的 Excel 数据入库 | 高 | ✅ 已完成 (dts_parser.py) |
| T2 | SKU 抓取纯xdotool方案重新设计 | 高 | ⏳ 待实现 |
| T3 | 京东平台搜索适配（当前仅淘宝搜索链路通畅） | 中 | ⏳ |
| T4 | 前端消息/通知中心完善 | 低 | ⏳ |
| T5 | 个人微信真实推送通道对接（当前仅记录日志） | 中 | ⏳ |
| T6 | DTS按钮坐标自动校准（图像识别替代固定坐标） | 中 | ⏳ |
