电商低价监控系统功能升级开发指令文档
版本：v3.0
更新日期：2026 年 6 月 8 日
项目路径：/home/lab-admin/price-monitor
核心目标：从简单 Excel 导入升级为以监控商品为中心的全流程管理系统，实现数据清洗、预览筛选、SKU 自动抓取、规格标准化、多维度预警等核心功能
给 Claude 的前置提示词
你好，这是电商低价监控系统的功能升级开发指令文档。基于之前完成的基础导入功能，我需要你按照以下要求进行全面升级：
先通读本文档所有内容，理解核心业务逻辑和用户需求
严格保留之前已完成的功能（Excel 导入、数据解析、基础预警），在此基础上进行扩展
先在 Plan Mode 下生成详细执行计划，包括：
需要新增的数据模型
需要修改的后端接口
需要新增的前端页面
测试步骤和验收标准
我审核计划无误后，你再切换到 Auto Mode 执行
所有代码必须严格遵守项目的 15 条核心公理和 GitHub 提交规范
所有新增功能必须有完整的日志记录和错误处理
一、核心业务逻辑变更
1.1 系统核心定位
从 "关键词搜索监控" 转变为 **"特定商品全平台价格监控"**
核心实体：监控商品（如 "蒙牛一米八八儿童奶粉"）
每个监控商品关联多个导入批次（Excel 文件）
每个导入批次关联多个淘宝商品（商品 ID）
每个淘宝商品关联多个 SKU 规格
1.2 完整业务流程
plaintext
用户创建监控商品 → 手动下载DTS Excel → 选择对应监控商品导入 → 
系统自动清洗广告数据 → 生成预览列表（含图片）→ 用户筛选剔除无关商品 → 
系统自动抓取每个商品的SKU详情 → SKU规格标准化和价格折算 → 
用户审核确认SKU分类 → 数据入库 → 触发价格/销量预警 → 
用户查看警告并标记处理
二、数据模型升级（优先级：最高）
2.1 新增数据表
文件路径：src/prisma/schema.prisma
prisma
// 监控商品主表（核心）
model MonitorProduct {
  id            Int      @id @default(autoincrement())
  name          String   // 监控商品名称（如"蒙牛一米八八儿童奶粉"）
  description   String?  // 商品描述
  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now())
  
  // 关联
  importBatches ImportBatch[]
  skuCategories SkuCategory[]
  alerts        Alert[]
}

// 导入批次表
model ImportBatch {
  id              Int      @id @default(autoincrement())
  monitorProductId Int     // 关联的监控商品ID
  fileName        String   // 导入的Excel文件名（含时间信息）
  importTime      DateTime @default(now()) // 导入时间
  totalCount      Int      // 总记录数
  adCount         Int      // 广告位数量
  validCount      Int      // 有效记录数
  importedBy      Int      // 导入用户ID
  
  // 关联
  monitorProduct MonitorProduct @relation(fields: [monitorProductId], references: [id])
  taobaoProducts TaobaoProduct[]
}

// 淘宝商品表
model TaobaoProduct {
  id            Int      @id @default(autoincrement())
  productId     String   @unique // 淘宝商品ID（13位数字，唯一）
  importBatchId Int      // 导入批次ID
  title         String   // 商品标题
  imageUrl      String   // 商品图片链接
  shopName      String   // 店铺名称
  sellerName    String   // 掌柜名
  price         Float    // 现价
  salesVolume   Int      // 总销量
  dailySales    Float    // 日均付款人数
  productUrl    String   // 商品链接
  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now())
  
  // 关联
  importBatch ImportBatch @relation(fields: [importBatchId], references: [id])
  skus        ProductSku[]
  alerts      Alert[]
}

// SKU分类表（规格标准化）
model SkuCategory {
  id              Int      @id @default(autoincrement())
  monitorProductId Int     // 关联的监控商品ID
  name            String   // 分类名称（如"袋装"、"罐装"、"混合装"）
  unit            String   // 单位（如"袋"、"罐"）
  conversionFactor Float  // 折算系数（用于计算单单位价格）
  createdAt       DateTime @default(now())
  
  // 关联
  monitorProduct MonitorProduct @relation(fields: [monitorProductId], references: [id])
  productSkus    ProductSku[]
}

// 商品SKU表
model ProductSku {
  id            Int      @id @default(autoincrement())
  taobaoProductId Int     // 淘宝商品ID
  skuCategoryId Int?     // 关联的SKU分类ID（可空，待审核）
  skuName       String   // SKU名称
  skuImageUrl   String?  // SKU图片链接
  skuPrice      Float    // SKU价格
  unitPrice     Float?   // 折算后的单单位价格（如单袋价格）
  isVerified    Boolean  @default(false) // 是否已审核确认分类
  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now())
  
  // 关联
  taobaoProduct TaobaoProduct @relation(fields: [taobaoProductId], references: [id])
  skuCategory   SkuCategory?  @relation(fields: [skuCategoryId], references: [id])
}

// 预警记录表（扩展原有Alert表）
model Alert {
  id            Int      @id @default(autoincrement())
  monitorProductId Int?   // 关联的监控商品ID（可选）
  taobaoProductId Int    // 关联的淘宝商品ID
  alertType     String   // 预警类型："price"（低价）、"sales"（销量突增）
  alertValue    Float    // 触发预警的值（价格/日均销量）
  threshold     Float    // 设置的预警阈值
  isHandled     Boolean  @default(false) // 是否已处理
  handledAt     DateTime? // 处理时间
  createdAt     DateTime @default(now())
  
  // 关联
  monitorProduct MonitorProduct? @relation(fields: [monitorProductId], references: [id])
  taobaoProduct TaobaoProduct @relation(fields: [taobaoProductId], references: [id])
}
2.2 数据库迁移
执行以下命令生成并应用迁移：
bash
运行
cd /home/lab-admin/price-monitor
./venv/bin/python -m prisma migrate dev --name v3_upgrade
三、后端功能升级（优先级：高）
3.1 监控商品管理接口
文件路径：src/api/monitor_products.py（新建文件）
功能：
新增、编辑、删除、查询监控商品
管理每个监控商品的 SKU 分类（袋装、罐装等）
3.2 导入流程重构
文件路径：src/api/products.py（修改原有导入接口）
核心修改：
导入接口改为关联监控商品 ID
python
运行
@router.post("/import-excel/{monitor_product_id}", dependencies=[Depends(_check_write_permission)])
async def import_product_excel(
    monitor_product_id: int,
    file: UploadFile = File(...)
) -> Dict[str, Any]:
新增数据清洗逻辑：根据占位类型字段过滤广告
python
运行
# 数据清洗：只保留"自然位"数据
cleaned_data = [item for item in parsed_data if item.get("占位类型") == "自然位"]
ad_count = len(parsed_data) - len(cleaned_data)
自动补全商品链接：添加https:前缀
python
运行
for item in cleaned_data:
    if item.get("商品链接") and not item["商品链接"].startswith("http"):
        item["商品链接"] = "https:" + item["商品链接"]
返回预览数据（含图片、标题、价格等）供用户筛选
python
运行
return {
    "code": 200,
    "msg": "数据解析成功",
    "data": {
        "file_name": file.filename,
        "total_count": len(parsed_data),
        "ad_count": ad_count,
        "valid_count": len(cleaned_data),
        "preview_data": cleaned_data  # 预览数据
    }
}
新增确认导入接口：接收用户筛选后的商品 ID 列表
python
运行
@router.post("/confirm-import/{monitor_product_id}", dependencies=[Depends(_check_write_permission)])
async def confirm_import(
    monitor_product_id: int,
    data: Dict[str, Any]
) -> Dict[str, Any]:
    """
    确认导入用户筛选后的商品
    请求体：{
        "file_name": "xxx.xlsx",
        "selected_product_ids": ["1234567890123", ...]
    }
    """
3.3 SKU 自动抓取功能
文件路径：src/services/sku_crawler.py（新建文件）
功能：
接收淘宝商品链接
使用纯 xdotool 物理操作打开商品详情页
自动抓取：
已售数量
所有 SKU 名称
每个 SKU 的价格
SKU 图片链接
返回结构化 SKU 数据
技术要求：
全程使用 xdotool 纯物理操作，不使用 CDP
加入随机延迟，模拟真人操作
处理可能出现的滑块验证
错误重试机制（最多 3 次）
3.4 SKU 规格标准化
文件路径：src/services/sku_normalizer.py（新建文件）
功能：
根据监控商品预定义的 SKU 分类（袋装、罐装等）
自动匹配 SKU 名称，归类到对应分类
计算单单位价格（如单袋价格 = SKU 价格 / 袋数）
无法自动匹配的 SKU 标记为待审核
记录已审核的 SKU 分类规则，下次自动应用
3.5 预警系统升级
文件路径：src/scripts/check_alerts.py（修改原有预警脚本）
核心修改：
支持按 SKU 设置价格红线
支持按监控商品设置日均销量红线
计算每个商品的日均付款人数
生成带原因的预警记录
支持批量标记预警为已处理
3.6 新增接口列表
表格
接口路径	方法	功能
/api/monitor-products	GET	获取所有监控商品
/api/monitor-products	POST	新增监控商品
/api/monitor-products/{id}	PUT	修改监控商品
/api/monitor-products/{id}	DELETE	删除监控商品
/api/monitor-products/{id}/sku-categories	GET	获取监控商品的 SKU 分类
/api/monitor-products/{id}/sku-categories	POST	新增 SKU 分类
/api/products/import-excel/{monitor_product_id}	POST	上传 Excel 并返回预览数据
/api/products/confirm-import/{monitor_product_id}	POST	确认导入筛选后的商品
/api/taobao-products/{id}/skus	GET	获取淘宝商品的 SKU 信息
/api/taobao-products/{id}/skus/verify	POST	审核确认 SKU 分类
/api/alerts/batch-handle	POST	批量标记预警为已处理
四、前端界面全面改造（优先级：中）
4.1 新增监控商品管理页面
文件路径：src/app/admin/monitor-products/page.tsx（新建）
功能：
监控商品列表（增删改查）
每个商品的 SKU 分类管理（添加、编辑、删除分类）
查看每个监控商品的导入历史和预警记录
4.2 重构商品导入流程
文件路径：src/app/admin/products/page.tsx（修改）
新流程：
选择要导入的监控商品
上传 Excel 文件
系统自动解析并显示预览页面：
显示文件名和导入时间
显示总记录数、广告数、有效数
列表展示有效商品（含图片、标题、价格、店铺）
每个商品前有复选框，默认全部勾选
用户取消勾选无关商品
点击 "确认导入" 按钮
系统自动抓取 SKU 详情并显示进度
导入完成后跳转到商品列表
4.3 新增 SKU 审核页面
文件路径：src/app/admin/products/[id]/skus/page.tsx（新建）
功能：
显示淘宝商品的所有 SKU 信息
自动归类的 SKU 显示对应分类
待审核的 SKU 允许用户选择分类
支持批量审核 SKU
审核通过后自动计算单单位价格
4.4 升级预警管理页面
文件路径：src/app/admin/alerts/page.tsx（修改）
新增功能：
按监控商品筛选预警
按预警类型（低价 / 销量）筛选
按处理状态筛选
显示预警原因和阈值
支持批量勾选预警并标记为已处理
预警记录显示对应的商品图片和链接
4.5 新增商品价格分析页面
文件路径：src/app/admin/analysis/page.tsx（新建）
功能：
选择监控商品和 SKU 规格
显示全淘宝平台该规格的价格分布
按价格从低到高排序
显示每个商品的店铺名称、价格、销量
支持按店铺名称、掌柜名筛选
五、核心功能详细说明
5.1 数据清洗规则
只保留占位类型字段值为 "自然位" 的记录
过滤掉商品 ID 为空或格式不正确的记录
过滤掉价格为 0 或负数的记录
自动补全商品链接（添加https:前缀）
5.2 SKU 自动匹配规则
预定义 SKU 分类关键词：
袋装：包含 "袋"、"袋装"、"条" 等关键词
罐装：包含 "罐"、"罐装"、"听" 等关键词
混合装：包含 "混合"、"组合"、"套装" 等关键词
自动提取 SKU 名称中的数量信息（如 "2 袋"、"3 罐"）
计算单单位价格：单单位价格 = SKU 价格 / 数量
无法匹配的 SKU 标记为 "待审核"，由用户手动分类
5.3 日均销量计算规则
首次导入：日均销量 = 总销量 / 商品上架天数（估算）
后续导入：日均销量 =(本次总销量 - 上次总销量)/ 两次导入间隔天数
保留最近 30 天的日均销量数据，计算平均值
5.4 预警触发规则
低价预警：SKU 价格 < 用户设置的价格红线
销量预警：日均销量 > 用户设置的销量红线
同一商品同一类型预警 24 小时内只触发一次
已标记为处理的预警不再提醒
六、测试验收标准
6.1 监控商品管理
✅ 可以新增、编辑、删除监控商品
✅ 可以为每个监控商品添加 SKU 分类
✅ SKU 分类可以设置单位和折算系数
6.2 导入流程
✅ 可以选择监控商品并上传 Excel 文件
✅ 系统自动过滤广告数据，显示预览列表
✅ 预览列表显示商品图片、标题、价格、店铺
✅ 可以取消勾选无关商品
✅ 确认导入后系统自动抓取 SKU 详情
✅ 导入完成后数据正确入库
6.3 SKU 管理
✅ 系统自动匹配 SKU 分类
✅ 自动计算单单位价格
✅ 待审核 SKU 可以手动选择分类
✅ 审核通过后下次自动应用相同规则
6.4 预警系统
✅ 可以为 SKU 设置价格红线
✅ 可以为监控商品设置销量红线
✅ 低于价格红线或高于销量红线触发预警
✅ 预警显示原因和阈值
✅ 可以批量标记预警为已处理
6.5 数据分析
✅ 可以按监控商品和 SKU 规格筛选
✅ 显示全平台价格分布
✅ 支持按店铺名称、掌柜名筛选
✅ 数据按价格从低到高排序
七、注意事项
SKU 抓取性能：批量导入时控制并发数量，避免触发淘宝反爬
数据一致性：导入过程中出现错误时，支持断点续传和回滚
用户体验：长时间操作（如批量抓取 SKU）显示进度条
错误处理：所有操作都有明确的错误提示和日志记录
权限控制：只有管理员和主管可以导入数据和审核 SKU
