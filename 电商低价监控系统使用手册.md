# 电商低价监控系统・完整使用手册

**版本**：v1.1

**更新日期**：2026 年 6 月 6 日

**项目路径**：`/home/lab-admin/price-monitor`

**运行账户**：lab-admin

**服务地址**：前端 `http://服务器IP:3000`

***

## 目录

1. [系统整体架构说明](#1-系统整体架构说明)
2. [前期基础配置（首次部署必做）](#2-前期基础配置首次部署必做)
3. [日常操作全流程](#3-日常操作全流程)
4. [前端后台使用指南](#4-前端后台使用指南)
5. [定时任务说明](#5-定时任务说明)
6. [定时任务一键重装](#6-定时任务一键重装)
7. [常见问题排查](#7-常见问题排查)
8. [目录速查](#8-目录速查)

***

## 1. 系统整体架构说明

本系统专为分销商价格管控设计，采用 **纯 xdotool 物理键盘鼠标操作** 技术，100% 绕过淘宝反爬检测。

**核心技术栈：xdotool（键盘鼠标）+ python-xlib（滑块验证）+ xclip（中文输入）+ openpyxl（Excel解析）**

**绝对不使用：** CDP远程调试、Selenium、Playwright、无头浏览器、自动化登录填表。

| 模块 | 功能说明 |
| ---- | ------------------------------ |
| 采集模块 | 定时抓取淘宝商品数据和价格（DTS店透视导出+解析） |
| 存储模块 | PostgreSQL 持久化商品、SKU、历史价格、预警数据 |
| 预警模块 | 低价预警 / 销量突增预警，工作时段飞书推送 |
| 后台管理 | 前端页面管理商品、查看预警、数据导出 |
| 运维模块 | 自动部署、定时任务、日志轮转、数据库自动备份 |

**数据流：** 搜索关键词 → DTS店透视市场分析 → 分批自动加载全量数据 → 导出Excel → 解析入库

***

## 2. 前期基础配置（首次部署必做）

### 2.1 飞书推送配置

1. 编辑环境变量文件：

```
nano /home/lab-admin/price-monitor/.env
```

2. 填入飞书机器人 Webhook 地址：

```
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/你的Webhook地址
```

3. 保存退出：`Ctrl+O` → 回车 → `Ctrl+X`

> **说明**：未配置时仅记录日志，不会发送消息；非工作时段 (9:00-18:00) 自动跳过推送。

### 2.2 系统阈值配置

在数据库 `SystemConfig` 表配置全局预警阈值：

- `alert_price`：低价预警门槛（低于该价格触发提醒）
- `sales_growth_threshold`：销量涨幅阈值（百分比）
- `sku_crawl_limit`：每日最大 SKU 抓取数量
- `sku_crawl_interval`：商品间抓取间隔（秒）

### 2.3 浏览器登录（防爬必备）

在服务器桌面打开 Chrome 浏览器（必须 DISPLAY=:0 真实显示器），**扫码登录淘宝账号**。

> **重要：**
> - Chrome 由 Supervisor 守护进程 `chrome-display` 自动管理
> - Cookie 自然持久化，只要不手动退出，后续抓取复用已登录会话
> - **Chrome 不得使用 `--remote-debugging-port` 参数**（淘宝100%检测封锁）
> - 启动参数已固化在 `scripts/ensure-chrome.sh`，不要手动修改

### 2.4 DTS 店透视扩展

必须安装 Chrome 扩展「店透视」（ID: `ppgdlgnehnajbbngnohepfigdmjbdpfb`），并登录店透视账号。

在 `.env` 中配置：
```
DIANTOUSHI_ACCOUNT=你的手机号
DIANTOUSHI_PASSWORD=你的密码
```

***

## 3. 日常操作全流程

### 3.1 服务启停 & 状态查看

#### 查看所有服务状态

```
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf status
```

输出示例：
```
fastapi-backend    RUNNING   pid 12345, uptime 1:23:45
nextjs-frontend    RUNNING   pid 12346, uptime 1:23:45
chrome-display     RUNNING   pid 12347, uptime 1:23:45
```

#### 重启单个服务

```
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart fastapi-backend
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart nextjs-frontend
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart chrome-display
```

#### 手动部署（更新代码后）

```
cd /home/lab-admin/price-monitor
scripts/deploy.sh
```

### 3.2 商品管理流程

#### 步骤 1：导入待监控商品

通过后台前端或脚本批量导入商品信息，必填字段：

- `product_id`：淘宝商品 ID（唯一标识，13位数字）
- `title`：商品名称
- `shop_name`：店铺名称

#### 步骤 2：商品审核

进入前端后台，对导入商品执行审核操作：

- ✅ **审核通过**：`is_approved = true`，纳入监控范围
- ⚪ **加入白名单**：`is_whitelist = true`，不触发低价预警
- ❌ **拒绝**：删除该商品及所有相关记录

> **核心规则**：仅「已审核 + 非白名单」商品参与价格和销量预警检测。

### 3.3 手动触发关键词搜索（抓取商品数据）

#### 通过前端触发

进入「关键词监控」页面 → 点击"全部立即搜索"或单行"🔍 立即搜索"

#### 通过 API 触发

```bash
curl -X POST http://localhost:3001/api/keywords/search \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"keyword_id": null}'
```

#### 搜索流程（全自动）

1. 检查 Chrome 是否运行（纯物理模式，无CDP端口）
2. 处理弹窗（恢复页面、DTS权限等）
3. 验证登录状态
4. xdotool 地址栏导航到 `s.taobao.com/search?q=关键词`
5. 等待搜索结果渲染（8-12秒）
6. 检测并处理滑块验证码（如有，python-xlib拖拽求解）
7. 点击 DTS 扩展图标 → 市场分析 → 开始分析
8. 分批自动加载全量数据（25s/次，8次/批，批间暂停325s）
9. 全选 → 导出Excel → 解析入库

### 3.4 预警检测手动执行

#### 测试模式（仅检测 + 入库，不发飞书）

```
venv/bin/python src/scripts/check_alerts.py --test
```

#### 正常模式（检测 + 入库 + 符合条件则推送飞书）

```
venv/bin/python src/scripts/check_alerts.py
```

#### 强制模式（忽略 24 小时去重限制，强制检测推送）

```
venv/bin/python src/scripts/check_alerts.py --force
```

#### 查看预警日志

```
# 实时查看最新日志
tail -f logs/alert.log

# 查看历史日志
cat logs/alert_YYYYMMDD.log
```

### 3.5 查看采集日志

```
# 实时查看最新日志
tail -f logs/crawl.log

# 查看 Chrome 日志
tail -f logs/chrome.log

# 查看后端日志
tail -f logs/fastapi-backend.log
```

***

## 4. 前端后台使用指南

**访问地址**：`http://服务器IP:3000`

### 4.1 关键词监控页 ★

**地址**：`http://服务器IP:3000/admin/keywords`

**功能清单：**

1. **关键词管理**：添加、编辑、启用/禁用关键词
2. **搜索触发**：
   - "全部立即搜索" — 搜索所有活跃关键词
   - 每行 "🔍 立即搜索" — 搜索单个关键词
3. **状态轮询**：自动每3秒轮询搜索状态
   - 🔵 蓝色：搜索中
   - 🟢 绿色：完成（显示入库商品数）
   - 🔴 红色：失败

### 4.2 预警管理页

**地址**：`http://服务器IP:3000/admin/alerts`

**功能清单：**

1. **顶部统计卡片**：总预警数、未读数量、7 日价格预警、7 日销量预警
2. **筛选功能**：按预警类型（价格 / 销量）、是否已读筛选
3. **列表展示**：
   - 🔴 红色标签：**低价预警**
   - 🟠 橙色标签：**销量预警**
   - 🔵 蓝色高亮：未读预警
4. **操作功能**：
   - 标记为已读：支持单条和批量操作
   - 导出 CSV：全量预警数据导出（兼容中文 Excel）
   - 查看商品：一键跳转到淘宝商品详情页

### 4.3 系统设置页

**地址**：`http://服务器IP:3000/admin/settings`

5 个标签页：
- **服务控制**：查看/重启服务状态
- **Cron 定时**：配置采集周期和频率
- **预警阈值**：设置价格和销量预警阈值
- **推送设置**：配置飞书/企业微信 Webhook
- **淘宝登录**：查看登录状态、触发扫码登录

### 4.4 故障排查页 ★

**地址**：`http://服务器IP:3000/admin/diagnostics`

提供系统健康检查、日志查看、维护操作（备份/重启/清理）。

***

## 5. 定时任务说明（全自动规则）

执行 `crontab -l` 可查看全部定时任务，**原有 OpenClaw 任务保留不冲突**。

| 执行周期 | 任务内容 | 日志路径 |
| -------- | -------------------- | ----------------- |
| 每 2 小时 | 全量商品 SKU / 价格 / 销量采集 | `logs/crawl.log` |
| 每 3 小时 | 价格 + 销量预警检测 + 飞书推送 | `logs/alert.log` |
| 每日 01:00 | PostgreSQL 数据库全量备份 | `logs/backup.log` |
| 每日 02:00 | 自动清理 7 天前日志文件 | - |

### 备份文件位置

```
/home/lab-admin/price-monitor/backups/YYYYMMDD.dump
```

***

## 6. 定时任务一键重装（换服务器 / 恢复配置）

### 通过 API 配置（推荐）

在前端「系统设置 → Cron 定时」页面修改配置后自动生效。

### 手动安装脚本

```
cd /home/lab-admin/price-monitor
chmod +x setup_cron.sh
./setup_cron.sh
```

脚本会自动：
1. 创建日志和备份目录
2. 检查并启动 cron 服务
3. 备份当前定时任务
4. 追加电商监控的定时任务（不覆盖原有任务）

***

## 7. 常见问题排查

### 7.1 搜索结果不显示商品卡片

**原因**：Chrome 使用了 `--remote-debugging-port` 参数（淘宝检测封锁）

**解决**：
```bash
# 检查 Chrome 进程参数
ps aux | grep chrome | grep remote-debugging

# 如有 --remote-debugging-port，重启 Chrome
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart chrome-display
```

### 7.2 抓取提示「DTS导出失败」

**可能原因**：
1. Chrome 未运行 → 检查 `supervisorctl status chrome-display`
2. 店透视扩展未安装或未登录 → 在Chrome中检查扩展状态
3. 淘宝账号未登录 → 在Chrome中确认登录状态
4. 滑块验证码拦截 → 系统会自动求解，如多次失败则需人工介入

### 7.3 滑块验证码频繁出现

**原因**：操作频率过高触发淘宝风控

**解决**：
- 系统已内置 325 秒批间暂停，自动控制频率
- 如仍频繁出现，增加关键词间搜索间隔
- 确保 Chrome 有真实人类浏览行为（偶尔手动操作一下页面）

### 7.4 预警检测结果为 0

**可能原因**：
1. 暂无商品低于价格阈值或销量无明显涨幅
2. 商品未审核或已加入白名单，不参与预警
3. 系统中未导入任何监控商品

### 7.5 飞书收不到消息

**排查步骤**：
1. 检查 `.env` 内 `FEISHU_WEBHOOK_URL` 是否正确
2. 检查当前时间是否在工作时段 9:00-18:00
3. 执行 `--test` 模式验证脚本本身无报错
4. 检查飞书机器人是否在目标群聊中

### 7.6 定时任务不执行

**排查步骤**：
1. 检查 cron 服务状态：`sudo systemctl status cron`
2. 检查脚本绝对路径是否与当前服务器路径一致
3. 手动执行对应脚本，确认脚本本身可运行
4. 查看 cron 系统日志：`tail -f /var/log/cron.log`

### 7.7 Chrome 窗口异常

**症状**：Chrome 弹出"要恢复页面吗"、扩展图标消失、窗口位置异常

**解决**：
```bash
# 重启 Chrome 守护进程（会自动清理崩溃标记）
sudo supervisorctl -c /home/lab-admin/price-monitor/supervisor.conf restart chrome-display
```

### 7.8 数据库恢复

```
pg_restore -U postgres -d openclaw -n price_monitor 备份文件路径.dump
```

***

## 8. 目录速查

```
price-monitor/
├── .github/
│   └── COMMIT_CONVENTION.md   # 代码提交规范
├── scripts/
│   ├── deploy.sh              # 自动部署脚本
│   ├── ensure-chrome.sh       # Chrome 守护进程脚本
│   └── human_crawl.py         # 人机协作爬虫脚本
├── src/
│   ├── api/                   # 后端API接口（18个路由）
│   │   ├── keywords.py        # ★ 关键词搜索
│   │   ├── taobao.py          # ★ 淘宝登录管理
│   │   ├── alerts.py          # 预警管理
│   │   ├── products.py        # 商品管理
│   │   ├── settings.py        # 系统设置
│   │   ├── cron.py            # 定时任务管理
│   │   └── ...
│   ├── app/                   # 前端页面 (Next.js 14)
│   │   └── admin/
│   │       ├── keywords/      # ★ 关键词监控
│   │       ├── alerts/        # 预警中心
│   │       ├── settings/      # 系统设置
│   │       ├── diagnostics/   # ★ 故障排查
│   │       └── ...
│   ├── services/              # 服务层
│   │   ├── xdotool_crawler.py # ★ 核心爬虫（纯xdotool）
│   │   ├── dts_parser.py      # ★ DTS Excel解析
│   │   ├── page_verifier.py   # ★ 页面状态验证
│   │   ├── human_behavior.py  # 真人行为模拟
│   │   ├── captcha_solver.py  # 滑块验证求解
│   │   ├── ga_service.py      # 编排服务
│   │   ├── product_service.py # 商品入库
│   │   ├── push_service.py    # 多通道推送
│   │   ├── auth_service.py    # 认证授权
│   │   ├── cdp_crawler.py     # 🔴 废弃
│   │   └── tagui_crawler.py   # 🔴 废弃
│   ├── ga_skills/             # 自动化技能（均已废弃）
│   ├── scripts/               # 采集/预警脚本
│   │   ├── check_alerts.py    # 预警检测
│   │   └── run_sku_crawl.py   # SKU抓取（待重设计）
│   ├── generated/             # Prisma自动生成客户端
│   └── prisma/                # 数据库模型&迁移
├── data/
│   └── downloads/             # DTS导出Excel存档
├── logs/                      # 运行日志
├── backups/                   # 数据库备份
├── .env                       # 环境变量（飞书Webhook等）
├── supervisor.conf            # ★ 进程守护配置
├── CLAUDE.md                  # ★ AI开发指令
├── 项目交接文档.md
└── 电商低价监控系统使用手册.md
```

> ★ = 核心文件，修改前务必了解上下文
> 🔴 = 已废弃模块，不再被任何生产代码导入

> （注：文档部分内容可能由 AI 生成）
